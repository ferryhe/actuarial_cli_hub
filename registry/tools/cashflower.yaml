id: actuarial.cashflow.cashflower
version: actuarial-cashflow.v1
name: Cashflower Cash-flow Runner
status: experimental
category: cashflow
priority: P1
upstream:
  repo: https://github.com/acturtle/cashflower
  package: cashflower
  language: python
  license: MIT
  install: 'pip install cashflower'
conversion:
  class: python-model-runner
  form: model-run wrapper for cash-flow models and assumptions
  dependencyWeight: medium
  readiness: Runtime wrapper, YAML assumptions fixture, JSON envelope behavior, and focused tests are implemented.
runtime:
  kind: cli
  command:
    - actuary
    - cashflow
    - cashflower
  availability: implemented
  allowedCommandPrefix: actuary cashflow cashflower
  timeoutMs: 300000
  maxOutputBytes: 1048576
io:
  inputSchema: registry/schemas/model-run-input.schema.json
  outputSchema: registry/schemas/model-run-result.schema.json
  artifacts:
    - deterministic_result
    - diagnostics
agent:
  skillPath: skills/hermes/cashflow-cashflower/SKILL.md
  useCases:
    - Cash-flow model demo runs
    - Agent-readable model output artifacts
  limitations:
    - Model code is user supplied and must be reviewed
    - Wrapper runs user supplied model.py files in an isolated temporary directory but does not sandbox arbitrary Python code
